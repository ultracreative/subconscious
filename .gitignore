# Rust build output
/target
**/*.rs.bk

# Commit Cargo.lock (this workspace builds the subc-core binary).

.DS_Store

# Agent runtime + local tooling installs (reinstallable, not project deliverables)
/.alfonso
/.opencode
/.agents
/_bmad
# Planning/decision artifacts kept LOCAL for now (Ufuk: not in the shared repo)
/_bmad-output

# Magic Context / CortexKit per-project module data (not source)
.cortexkit/

# Auto-generated docs from a background tooling pass (not authored source — kept out
# of the repo; same files appear in sibling repos from the same generator).
/ARCHITECTURE.md
/STRUCTURE.md
docs/private/
.build/
.wrangler/
# Arcus release artifacts and toolchain
/dist/
packages/arcus/toolchain/
/.omo/
/.ucs/
